
{
  "name": "webgl.js",
  "version": "0.0.1",
  "description": "JavaScript 3D library",
  "main": "bin/webgl.js",
  "scripts": {
    "build": "cd ./utils/build && node build.js --include common",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "marius oprea",
  "license": "free",
  "devDependencies": {
    "argparse": "^1.0.3",
    "jscs": "^1.13.1",
    "uglify-js": "^2.6.0"
  }
}